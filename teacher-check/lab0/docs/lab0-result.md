# Lab0 结果

## 目标

完成课程代码阅读、环境自检，并整理 `echo` 控制流、`exec` 状态快照和时钟中断旅程三张图。

## 查验

在仓库根目录执行：

```bash
python3 code/tools/preflight.py
```

环境输出记录见 `environment-check.md`，对应终端截图为 `../images/lab0-terminal-run.png`。

## 结果

工具链和 QEMU 可用性已记录；三张黑白 Mermaid 图已导出为 PNG，并保留 `.mmd` 源文件；阅读依据和现场说明见 `lab0-reading-notes.md`。

归档标签：`lab0`。
