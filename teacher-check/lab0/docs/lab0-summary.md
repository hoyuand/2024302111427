# Lab0 结果摘要

目标：完成课程代码导读、环境自检，并绘制 `echo` 控制流、`exec` 地址空间快照和时钟中断时序图。

环境：WSL2；RISC-V 交叉工具链与 QEMU 可用性记录见 `environment-check.md`。

结果：三张图均保留 Mermaid 源文件、黑白 SVG 和 PNG；关键源码依据、自查题及现场问答要点见 `lab0-reading-notes.md`。

归档：`lab0` 标签对应本实验完成提交；图示仅用于解释，运行类结论以文字记录和后续 Lab1 的真实 QEMU 输出为准。
