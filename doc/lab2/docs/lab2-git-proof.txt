Lab2 Git archive proof
======================
Lab2 is submitted from the final local commit with the `lab2` and `lab2-submit`
tags. `lab2-start` marks the code baseline before the final evidence/documentation
commit. The complete archive and code-only increment archive are generated locally
and are not committed into the source tree.

Remote:
  https://github.com/hoyuand/2024302111427.git

Code/tree commit before this proof-only metadata commit:
  cddf29e535255c37139500bb06e5d3789e9eb3db

The final `main`, `lab2`, and `lab2-submit` refs are updated to the commit
containing this proof file. The remote object check immediately before this
proof-only update was:
  cddf29e535255c37139500bb06e5d3789e9eb3db  refs/heads/main
  43c675ba8dd5de7462d6a8b5d8127d2081a6bb55  refs/tags/lab2-start
  afbe8873d12df97b49a142dfef3c14e3b84c3a6b  refs/tags/lab2-start^{}
  cddf29e535255c37139500bb06e5d3789e9eb3db  refs/tags/lab2
  cddf29e535255c37139500bb06e5d3789e9eb3db  refs/tags/lab2-submit

The GitHub repository was renamed from `os-A---2024302111427` to the exact
student-ID name `2024302111427`; `origin` now uses the URL above.

Archive commands:
  git archive --format=zip -o 提交-lab2-2024302111427.zip lab2-submit
  git diff --name-only lab2-start lab2-submit -- code
  git archive --format=zip --prefix=lab2-code-delta-2024302111427/ -o code/archives/lab2-code-delta-2024302111427.zip lab2-submit code/Makefile code/kernel/console.c code/kernel/lab2.ld code/kernel/proc.c code/kernel/proc.h code/kernel/start.c code/kernel/syscall.c code/kernel/trampoline_anchor.S code/kernel/trap.c code/support/lab2-overflow.script code/support/lab2-spin.script

Push commands:
  git push origin main
  git push origin lab2-start lab2 lab2-submit

Verify the final pointers:
  git ls-remote origin refs/heads/main refs/tags/lab2-start^{} refs/tags/lab2^{} refs/tags/lab2-submit^{}
  git rev-parse HEAD 'lab2^{}' 'lab2-submit^{}' 'lab2-start^{}'
The `main`, `lab2`, and `lab2-submit` object IDs must equal the final submission
commit; `lab2-start` must equal the earlier code-baseline commit. The historical
Lab0/Lab1 refs are checked separately and are not overwritten by this Lab2 push.
