# 操作系统实践实验归档

完整说明、教师检查入口、每个 Lab 的图片与文档见 [README.md](README.md) 和 [teacher-check/](teacher-check/)。

每轮实验统一执行：

```bash
git add .
git commit -m "第n次实验：完成代码、测试和报告证据"
git tag labN
git push origin main
git push origin labN
git ls-remote --heads --tags origin refs/heads/main refs/tags/labN
git archive --format=zip -o 提交-labN-2024302111427.zip labN
```

标签在本轮全部材料完成后一次创建，不移动、不强制更新；本次 Lab1 仅因补齐教师检查目录做一次性标签校正，后续实验不再移动标签。实验截图和简要说明统一放入 `teacher-check/labN/images/` 与 `teacher-check/labN/docs/`。架构图使用 Mermaid、Graphviz 或手工矢量绘图导出，保留可编辑源文件；真实运行截图与示意图分开标注。
